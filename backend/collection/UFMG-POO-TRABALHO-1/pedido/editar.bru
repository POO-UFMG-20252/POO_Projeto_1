meta {
  name: editar
  type: http
  seq: 3
}

put {
  url: http://localhost:5000/api/pedido
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id": 1,
    "estado": 2
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
