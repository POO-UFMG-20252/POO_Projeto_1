meta {
  name: buscar_por_nome
  type: http
  seq: 5
}

get {
  url: http://localhost:5000/api/produto/buscar?q=para
  body: none
  auth: bearer
}

params:query {
  q: para
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
